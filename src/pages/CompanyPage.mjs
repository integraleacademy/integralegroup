import { card, title } from '../scripts/html.mjs';
import { company, values } from '../data/siteContent.mjs';
export const route = '/entreprise';
export const buttons = '<a class="btn primary" href="/contact">Nous contacter</a>';
export function render(){return `<section>${title('Présentation Intégrale Group','Intégrale Group','INTEGRALE GROUP est une SAS indépendante qui structure des projets dans la certification professionnelle, le digital, les solutions SaaS et l’accompagnement d’activités ambitieuses.')}</section><section>${title('Infos légales','Informations légales')}<div class="legalGrid">${Object.entries({'SAS':company.legal,'SIREN':company.siren,'SIRET':company.siret,'Adresse':company.address,'Capital social':company.capital,'Date de création':company.created,'Activité':company.activity,'Code NAF/APE':company.naf}).map(([k,v])=>`<p><b>${k}</b><span>${v}</span></p>`).join('')}</div></section><section class="band">${title('Vision','Notre vision','Créer des structures, sites et outils qui ne sont pas seulement beaux, mais exploitables, maintenables et utiles à la croissance.')}</section><section>${title('Valeurs','Nos valeurs')}<div class="grid three">${values.map(v=>card(v,'Une valeur intégrée dans la stratégie, le design, le contenu et l’exécution technique.')).join('')}</div></section><section>${title('Équipe','Une direction impliquée')}<article class="team"><div>CV</div><h3>Clément Vaillant</h3><strong>Président / Fondateur</strong><p>Pilotage stratégique, développement du groupe, certification professionnelle, digitalisation et solutions métiers.</p></article></section>`;}
