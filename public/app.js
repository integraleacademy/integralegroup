const header=document.querySelector('.siteHeader');const burger=document.querySelector('.burger');burger?.addEventListener('click',()=>{const open=header.classList.toggle('open');burger.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.navLinks a').forEach(a=>a.addEventListener('click',()=>header.classList.remove('open')));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const form=document.querySelector('#contactForm');if(form){const subject=new URLSearchParams(location.search).get('sujet')||new URLSearchParams(location.search).get('subject');if(subject){const map={partenariat:'Certification / partenariat',webdesign:'Webdesign',saas:'Développement SaaS','integrale-connect':'Intégrale Connect'};form.sujet.value=map[subject]||subject;}form.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(form);const body=['Nom: '+data.get('nom'),'Prénom: '+data.get('prenom'),'Entreprise: '+data.get('entreprise'),'Email: '+data.get('email'),'Téléphone: '+data.get('telephone'),'','Message:',data.get('message')].join('\n');location.href='mailto:clement@integraleacademy.com?subject='+encodeURIComponent(data.get('sujet'))+'&body='+encodeURIComponent(body);});}
